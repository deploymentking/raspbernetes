127.0.0.1 localhost
127.0.0.1 {{ r10s_hostname }}

# The following lines are desirable for IPv6 capable hosts
::1 ip6-localhost ip6-loopback
fe00::0 ip6-localnet
ff00::0 ip6-mcastprefix
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
ff02::3 ip6-allhosts

192.168.0.80 k8s-master k8s-master.thinkstack.io
{# Add an entry for each host in the cluster, using the loop index to map IPs #}
{% for host in groups['raspbernetes_workers'] %}
192.168.0.{{ 90 + loop.index - 1 }} k8s-worker{{ loop.index - 1 }} k8s-worker{{ loop.index - 1 }}.thinkstack.io
{% endfor %}
